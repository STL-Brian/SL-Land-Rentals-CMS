import Link from "next/link";import {formatMoneyMinor} from "@lake-tech/core";import type {Listing} from "../lib/listings";
export function RentalCard({item}:{item:Listing}){return <article className="card"><div className={`card-visual ${item.hero_gradient}`}/><div className="card-body"><span className={`badge ${item.available?"":"occupied"}`}>{item.available?"AVAILABLE":"OCCUPIED"}</span><h2 className="listing-name"><Link href={`/rentals/${item.slug}`}>{item.name}</Link></h2><p className="listing-description">{item.description}</p><div className="listing-meta"><p>{item.region_name} · {item.kind==="FULL_REGION"?"Full private region":"Estate parcel"}</p><div className="facts"><span>{item.area_sqm.toLocaleString()} m²</span><span>{item.prims.toLocaleString()} LI</span></div></div><div className="listing-pricing"><div className="price">L${item.weekly_linden.toLocaleString()} / week</div><p>${formatMoneyMinor(item.stripe_weekly_minor)} {item.stripe_currency.toUpperCase()} / week</p><p>One-time setup: L${item.setup_linden.toLocaleString()} / ${formatMoneyMinor(item.stripe_setup_minor)} {item.stripe_currency.toUpperCase()}</p></div></div></article>}
