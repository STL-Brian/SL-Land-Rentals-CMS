import {NextResponse} from "next/server";import {cookies} from "next/headers";import {hashSessionToken} from "@lake-tech/core";import {query} from "@lake-tech/db";import {assertBrowserOrigin} from "../../../../lib/http";import {env} from "../../../../lib/env";
export async function POST(req:Request){const cfg=env();assertBrowserOrigin(req.headers,cfg.baseUrl);const jar=await cookies();const token=jar.get("lte_session")?.value;if(token)await query("DELETE FROM sessions WHERE token_hash=$1",[hashSessionToken(token)]);const response=NextResponse.redirect(new URL("/",cfg.baseUrl),303);response.cookies.set("lte_session","",{path:"/",expires:new Date(0),httpOnly:true,sameSite:"lax",secure:cfg.cookieSecure});return response}
