import type { UserRole } from "@lake-tech/core";import { query } from "@lake-tech/db";import { requireViewer } from "../../../lib/auth";import { AppShell } from "../../../components/app-shell";import { UserRoleForm } from "../../../components/user-role-form";export const dynamic="force-dynamic";
export default async function Users(){const viewer=await requireViewer("user:manage");const users=await query<{id:string;display_name:string;canonical_username:string|null;avatar_id:string|null;role:UserRole;active:boolean;created_at:Date}>(`SELECT u.id,u.display_name,s.canonical_username,s.avatar_id,u.role,u.active,u.created_at FROM users u LEFT JOIN sl_identities s ON s.user_id=u.id ORDER BY u.created_at DESC`);return <AppShell viewer={viewer} eyebrow="Management / Access" title="Users & roles"><section className="panel card"><div className="section-head"><div><h2>Account directory</h2><p>Role changes revoke every active session.</p></div></div><div className="table-scroll"><table className="table table-dark table-hover align-middle mb-0"><thead><tr><th>Identity</th><th>Avatar UUID</th><th>Status</th><th>Role & action</th></tr></thead><tbody>{users.rows.map(u=><tr key={u.id}><td><strong>{u.display_name}</strong><small>{u.canonical_username??"Unlinked"}</small></td><td className="mono">{u.avatar_id??"—"}</td><td>{u.active?"Active":"Disabled"}</td><td><UserRoleForm id={u.id} current={u.role}/></td></tr>)}</tbody></table></div></section></AppShell>}
