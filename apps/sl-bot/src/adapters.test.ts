import {describe,expect,it,vi} from "vitest";import {retryDelaySeconds,withTimeout} from "./adapters.js";
describe("persistent outbox retries",()=>{it("uses bounded exponential retry",()=>{expect(retryDelaySeconds(0)).toBe(2);expect(retryDelaySeconds(3)).toBe(16);expect(retryDelaySeconds(20)).toBe(300)});it("bounds a hung adapter login/connect",async()=>{vi.useFakeTimers();const work=new Promise<void>(()=>undefined);const pending=withTimeout(work,100,"adapter connect timeout");const assertion=expect(pending).rejects.toThrow(/connect timeout/);await vi.advanceTimersByTimeAsync(101);await assertion;vi.useRealTimers()})});
